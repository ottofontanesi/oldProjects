{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-desktop",
  "description": "Main ResonantOS desktop shell permissions. Dialog open is required for user-selected Living Archive folders.",
  "windows": ["main", "floating-chat"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "dialog:allow-open"
  ]
}
