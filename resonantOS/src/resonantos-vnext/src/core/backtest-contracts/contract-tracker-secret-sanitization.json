{
  "id": "contract-tracker-secret-sanitization",
  "version": "1.0.0",
  "description": "All parameter values matching the secret deny-list names or secret value regex patterns are replaced with [REDACTED] before entering the buffer, while non-secret values and JSON structure are preserved unchanged",
  "category": "tool-call-tracker",
  "preconditions": [
    {
      "description": "A tool call record contains input parameters with potential secret values (API keys, tokens, passwords, PEM keys)"
    }
  ],
  "expectedOutcome": {
    "description": "Secret values are replaced with [REDACTED] and non-secret values remain unchanged; JSON structure (keys, nesting, array positions) is preserved",
    "assertion": "truthy"
  },
  "verificationMethod": {
    "type": "property-test",
    "testFile": "src-tauri/src/tool_call_sanitizer.rs",
    "testName": "Property 3: Secret sanitization completeness"
  },
  "referencedComponents": [
    "tool_call_sanitizer::sanitize_parameters",
    "tool_call_sanitizer::SECRET_PARAM_NAMES",
    "tool_call_sanitizer::SECRET_VALUE_PATTERNS"
  ],
  "createdAt": "2026-07-15T00:00:00Z",
  "updatedAt": "2026-07-15T00:00:00Z"
}
